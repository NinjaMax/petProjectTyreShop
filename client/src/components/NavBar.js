import React from 'react';
import {Context} from './index';

const NavBar = () => {
    const {user} = userContext(Context);
    return (
        <div>
            
        </div>
    );
};

export default NavBar;